import 'package:flutter/cupertino.dart';

class HotKeyManager extends StatelessWidget {
  const HotKeyManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
